/**
 * Export the default mounting point to be used with DPM BW REST API
 */
import env from "./env_constants";

export default {
/*
    getApiServerURL: function () {
        switch (env.getServerEnv()) {
            case env.LOCAL:
                return "http://localhost:8080";
            default:
                return "/api";
        }
    },

    getWSServerURL: function () {
        switch (env.getServerEnv()) {
            case env.LOCAL:
                return "http://localhost:9290/dpmws";
            default:
                return "/ws";
        }
    },

    APPLICATIONS: '/application',
    BASELINES: '/baselines',
    BASELINE: '/baseline',
    BASELINE_HISTORY: '/baselineDeploymentHistory',
    TRG_SESSION: '/trgsession',
    TRG_SESSION_APPLY_CONFIG: "/trgsession/apply",
    TRG_SESSION_STATUS_DEPLOY: id => `/trgsession/${id}/status/deploy`,
    DEPLOYMENT_JOB_REPORT: jobId => `/jobreport?jobId=${jobId}`,
    TRG_TEMPLATE: "/trgtemplate",
    GROUPS: '/groups',
    DEPLOY: '/deploy',
    HOSTS: '/hosts',
    AIRSPACES: "/airspaces",
    EXERCISES: "/exercises",
    WEATHER: "/weather",
    PILOTS: "/pilots",

    CONTROLLERS: "/controllers",
    TOPOLOGY: "/topology",
    DEF_SAPS: "/defaultsaps",
    DPM_APP_VERSION: "/dpmApplicationVersion"*/
};